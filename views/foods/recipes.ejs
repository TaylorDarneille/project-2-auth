<h2>To Cook Recipes</h2>

<button>Remove</button>

<% allFoods.forEach((food) => { %>
    <% //if(food.name != 'undefined') { %>
        <div>
            <p><%= food.name %></p>
        </div>
    <% //} %>
<% }) %>